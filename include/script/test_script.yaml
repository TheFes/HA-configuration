test_script:
  alias: "ZZ 🧪 Script for quick tests"
  icon: mdi:test-tube
  sequence:
    - variables:
        extra_data:
          language: nl
          voice: thefes
    - service: tts.google_cloud_say
      data: >
        {{ dict(
                  entity_id = 'media_player.whatever',
                  message = 'test'
        )}}
