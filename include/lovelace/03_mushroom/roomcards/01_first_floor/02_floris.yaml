type: custom:vertical-stack-in-card
mode: vertical
card_mod:
  style: |
    ha-card {
      border: 0px;
      background: none;
      box-shadow: none;
    }
cards:
  - type: custom:mushroom-template-card
    primary: Floris
    icon: mdi:bed
    icon_color: blue
    layout: horizontal
    secondary: >-
      {{ states('sensor.floris_temp') }}°C | 
      {{ states('sensor.floris_lux') | int }} lx
    tap_action:
      action: navigate
      navigation_path: floris
    card_mod:
      style: |
        ha-card {
          border: 0px;
          background: none;
          box-shadow: none;
        }
  - type: custom:vertical-stack-in-card
    horizontal: true
    cards:
      - type: custom:mushroom-template-card
        entity: light.groep_floris
        layout: vertical
        icon: |-
          {% if is_state('light.groep_floris', 'on') %}
            mdi:lightbulb-group
          {% else %}
            mdi:lightbulb-group-off
          {% endif %}
        icon_color: |-
          {% if is_state('light.groep_floris', 'on') %}
            yellow
          {% endif %}
        card_mod:
          style: |
            ha-card {
              border: 0px;
              background: none;
              box-shadow: none;
            }
      - type: custom:mushroom-template-card
        entity: cover.floris
        layout: vertical
        icon: |-
          {% if is_state('cover.floris', 'open') %}
            mdi:blinds-open
          {% else %}
            mdi:blinds
          {% endif %}
        icon_color: |-
          {% if is_state('cover.floris', 'closed') %}
            yellow
          {% endif %}
      - type: conditional
        conditions:
          - entity: media_player.floris_mini
            state_not: "off"
          - entity: media_player.floris_mini
            state_not: "idle"
        card:
          type: custom:mushroom-template-card
          layout: vertical
          entity: media_player.floris_mini
          icon: mdi:cast
          icon_color: >-
            {% if is_state('media_player.floris_mini', 'playing') %}
              yellow
            {% endif %}
          style: |
            ha-card {
              background: none;
              box-shadow: none;
            }
