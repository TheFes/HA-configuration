platform: template
covers:
  pepijn:
    device_class: blind
    availability_template: "{{ not is_state('cover.pepijn_tahoma', 'unavailable') }}"
    friendly_name: "Screen Pepijn"
    position_template: >
      {% if is_state('input_boolean.cover_pepijn_invert', 'on') %}
        {{ 100 - state_attr('cover.pepijn_tahoma', 'current_position') | float(100) }}
      {% else %}
        {{ state_attr('cover.pepijn_tahoma', 'current_position') | float(0) }}
      {% endif %}
    open_cover:
      service: >
        {% if is_state('input_boolean.cover_pepijn_invert', 'on') %}
          cover.close_cover
        {% else %}
          cover.open_cover
        {% endif %}
      entity_id: cover.pepijn_tahoma
    close_cover:
      service: >
        {% if is_state('input_boolean.cover_pepijn_invert', 'on') %}
          cover.open_cover
        {% else %}
          cover.close_cover
        {% endif %}
      entity_id: cover.pepijn_tahoma
    stop_cover:
      service: cover.stop_cover
      entity_id: cover.pepijn_tahoma
    set_cover_position:
      service: cover.set_cover_position
      entity_id: cover.pepijn_tahoma
      data:
        position: >
          {% if is_state('input_boolean.cover_pepijn_invert', 'on') %}
            {{ 100 - position }}
          {% else %}
            {{ position }}
          {% endif %}
