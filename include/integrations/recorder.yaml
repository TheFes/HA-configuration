recorder:
  # db_url: !secret mariadb_url
  purge_keep_days: 2
  exclude:
    entities:
      - sensor.weather_forecasts_hourly
      - sensor.weather_forecasts_daily
    domains:
      - updater
      - scene
      - weblink
      - calendar
      - camera
      - weather
      - button
    entity_globs:
      - sensor.bm_*
      - light.bm_*
      - media_player.bm_*
