test_script:
  alias: "ZZ 🧪 Script for quick tests"
  icon: mdi:test-tube
  sequence:
    - variables:
        extra_data:
    - service: persistent_notification.create
      data:
        message: "value is {{extra_data}}"
